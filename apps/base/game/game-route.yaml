apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: game-route
  namespace: game
spec:
  parentRefs:
  - name: game-gateway
    sectionName: http

  hostnames:
    - game.x4.com 
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: game-service
          port: 80
          weight: 1
