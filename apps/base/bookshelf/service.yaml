apiVersion: v1
kind: Service
metadata:
  name: bookshelf
  namespace: bookshelf
spec:
  ports:
    - port: 80
      targetPort: 3005
      protocol: TCP 
  selector:
    app: bookshelf
  type: ClusterIP
