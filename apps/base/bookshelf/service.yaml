apiVersion: v1
kind: Service
metadata:
  name: bookshelf
  namespace: bookshelf
spec:
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP 
  selector:
    app: bookshelf
  type: ClusterIP
