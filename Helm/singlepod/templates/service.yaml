apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.svc_name }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}

  selector:
    app: {{ .Values.name }}
  type: ClusterIP

