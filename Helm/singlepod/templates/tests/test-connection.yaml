apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "singlepod.fullname" . }}-test-connection"
  labels:
    {{- include "singlepod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "singlepod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
